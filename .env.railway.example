# ==========================================
# 独角数卡 Railway 部署环境变量模板
# ==========================================
# 使用说明：
# 1. 复制此文件内容
# 2. 在 Railway 项目的 Variables 页面逐个添加
# 3. 将 {{占位符}} 替换为实际值
# ==========================================

# ---------- 基础应用配置 ----------
APP_NAME=独角数卡
APP_ENV=production
APP_KEY={{运行 php artisan key:generate --show 获取}}
APP_DEBUG=false
APP_URL={{你的 Railway 应用域名，例如：https://your-app.railway.app}}

LOG_CHANNEL=stack

# ---------- 数据库配置 ----------
# Railway MySQL 插件会自动提供这些变量，可以直接引用
DB_CONNECTION=mysql
DB_HOST=${{MYSQL.MYSQL_PRIVATE_URL}}
DB_PORT=3306
DB_DATABASE=railway
DB_USERNAME=root
DB_PASSWORD=${{MYSQL.MYSQL_PASSWORD}}

# 或者使用完整的 DATABASE_URL
# DATABASE_URL=${{MYSQL.DATABASE_URL}}

# ---------- Redis 配置 ----------
# 需要在 Railway 添加 Redis 插件
REDIS_HOST=${{REDIS.REDIS_PRIVATE_URL}}
REDIS_PASSWORD=${{REDIS.REDIS_PASSWORD}}
REDIS_PORT=6379
REDIS_DB=0
REDIS_CACHE_DB=1

# ---------- 缓存和队列配置 ----------
CACHE_DRIVER=redis
QUEUE_CONNECTION=redis
SESSION_DRIVER=file
SESSION_LIFETIME=120
BROADCAST_DRIVER=log

# ---------- 后台管理配置 ----------
DUJIAO_ADMIN_LANGUAGE=zh_CN
ADMIN_ROUTE_PREFIX=/admin
ADMIN_HTTPS=true

# ---------- 邮件配置（可选）----------
# MAIL_MAILER=smtp
# MAIL_HOST=smtp.example.com
# MAIL_PORT=587
# MAIL_USERNAME=your-email@example.com
# MAIL_PASSWORD=your-email-password
# MAIL_ENCRYPTION=tls
# MAIL_FROM_ADDRESS=noreply@example.com
# MAIL_FROM_NAME="${APP_NAME}"

# ---------- 极验证配置（可选）----------
# GEETEST_ID=your-geetest-id
# GEETEST_KEY=your-geetest-key
