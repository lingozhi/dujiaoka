# 支付宝回调诊断步骤

## ⚠️ 问题描述
- 支付成功
- 订单状态：待支付
- 卡密未显示

**原因**：支付宝异步回调（notify_url）没有成功处理订单

---

## 1️⃣ 查看 Railway 日志

访问 Railway Dashboard → 你的项目 → Logs

搜索关键词：`支付宝回调` 或 订单号 `TM1EU1JBPNACJAG3`

---

## 2️⃣ 可能的情况

### 情况 A：没有任何"支付宝回调接收"日志 ❌

**原因**：支付宝根本没有发送回调到服务器

**检查**：
1. 支付宝开放平台后台是否配置了回调地址
2. 回调地址是否使用了旧域名：
   - ❌ 旧域名：`https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url`
   - ✅ 新域名：`https://faka.opwan.ai/pay/alipay/notify_url`

**解决方法**：
1. 进入支付宝开放平台：https://open.alipay.com/
2. 控制台 → 选择应用 → 开发信息
3. 找到 **应用网关** 或 **异步通知地址**
4. 更新为：`https://faka.opwan.ai/pay/alipay/notify_url`
5. 保存配置

---

### 情况 B：有"支付宝回调接收"日志，但有"签名验证失败"错误 ⚠️

**日志示例**：
```
[INFO] 支付宝回调接收 {...}
[INFO] 支付宝回调配置 {...}
[ERROR] 支付宝回调异常: 验签失败
```

**原因**：merchant_key 配置不正确

**解决方法**：
1. 登录系统后台：`https://faka.opwan.ai/admin`
2. 店铺管理 → 支付方式管理 → 编辑支付宝
3. 检查 `merchant_key` 字段
4. 确保填的是**支付宝公钥**（从支付宝后台获取）
5. 不是应用公钥，不是应用私钥

**获取支付宝公钥**：
1. 支付宝开放平台 → 应用 → 开发信息
2. 接口加签方式 → 点击"查看支付宝公钥"
3. 复制公钥内容（不要包含头尾）

---

### 情况 C：有"签名验证成功"日志，但有"订单处理失败"错误 🔧

**日志示例**：
```
[INFO] 支付宝回调签名验证成功 {...}
[ERROR] 支付宝订单处理失败 {error: "具体错误信息"}
```

**原因**：completedOrder 方法执行时出错

**解决方法**：
- 查看日志中的具体错误信息
- 可能是数据库连接、卡密库存、邮件发送等问题

---

### 情况 D：有"订单处理成功"日志 ✅

**日志示例**：
```
[INFO] 支付宝回调接收 {...}
[INFO] 支付宝回调签名验证成功 {...}
[INFO] 支付宝订单处理成功 {...}
```

**原因**：回调已成功处理，可能是浏览器缓存

**解决方法**：
- 刷新订单页面（Ctrl + F5 强制刷新）
- 订单状态应该已经更新为"已完成"
- 卡密应该已显示

---

## 3️⃣ 快速测试回调地址是否可访问

在浏览器中直接访问：
```
https://faka.opwan.ai/pay/alipay/notify_url
```

**期望结果**：
- ✅ 返回 `error` 文本（说明路由正常）
- ❌ 返回 `404 Not Found`（说明路由有问题）

---

## 4️⃣ 检查环境变量

确保 Railway 中配置了正确的域名：

```
APP_URL=https://faka.opwan.ai
```

如果修改了环境变量，需要重新部署。

---

## 5️⃣ 支付宝回调重试机制

**重要**：支付宝会多次重试发送回调！

如果第一次回调失败，支付宝会在以下时间再次发送：
- 2分钟后
- 10分钟后
- 1小时后
- 2小时后
- 6小时后
- 15小时后

所以如果配置了正确的回调地址，即使现在订单未更新，等待几分钟后可能会自动更新。

---

## 🎯 建议的排查顺序

1. **立即检查**：访问 `https://faka.opwan.ai/pay/alipay/notify_url` 确认路由正常
2. **查看日志**：在 Railway 搜索 `TM1EU1JBPNACJAG3` 或 `支付宝回调`
3. **更新回调地址**：如果日志中没有回调记录，去支付宝后台更新回调地址为新域名
4. **等待重试**：支付宝会自动重试，等待2-10分钟
5. **测试新订单**：创建新的测试订单验证配置是否正确

---

## 💡 临时解决方案

如果急需发货，可以手动处理订单：

1. 登录后台：`https://faka.opwan.ai/admin`
2. 订单管理 → 找到订单 `TM1EU1JBPNACJAG3`
3. 编辑订单 → 修改状态为"已完成"
4. 系统会自动分配卡密并发送邮件

---

**请先查看 Railway 日志，然后告诉我具体看到了什么内容，我会帮你进一步排查！** 🔍
