# 支付宝支付配置完整指南

## 📋 目录
1. [支付宝开放平台配置](#支付宝开放平台配置)
2. [系统后台配置](#系统后台配置)
3. [字段详细说明](#字段详细说明)
4. [常见问题](#常见问题)

---

## 🔑 支付宝开放平台配置

### 步骤 1：注册并创建应用

1. **访问支付宝开放平台**
   - 网址：https://open.alipay.com/
   - 使用支付宝账号登录

2. **创建应用**
   - 控制台 → 网页&移动应用
   - 点击 **创建应用**
   - 选择应用类型：**网页应用** 或 **移动应用**
   - 填写应用信息并提交审核

3. **等待审核通过**
   - 审核时间：1-3个工作日
   - 审核通过后会分配 **App ID**

### 步骤 2：配置应用密钥

#### 2.1 下载密钥生成工具

访问：https://opendocs.alipay.com/common/02kipl

下载 **支付宝密钥生成器**（根据你的系统选择 Windows/Mac/Linux）

#### 2.2 生成密钥对

1. 打开密钥生成器
2. 选择密钥格式：**PKCS1 (非JAVA适用)** 或 **PKCS8 (JAVA适用)**
   - PHP 项目选择 **PKCS1**
3. 选择密钥长度：**2048**
4. 点击 **生成密钥**

生成后会得到：
- **应用私钥**（Private Key）- 保存好，不要泄露
- **应用公钥**（Public Key）- 需要上传到支付宝

#### 2.3 上传应用公钥到支付宝

1. 支付宝开放平台 → 控制台 → 选择应用
2. 进入 **开发信息** → **接口加签方式（密钥/证书）**
3. 点击 **设置** → 选择 **公钥**
4. 粘贴 **应用公钥**（整个内容，包括开头结尾）
   ```
   MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...（很长的一串）
   ```
5. 点击 **保存设置**

#### 2.4 获取支付宝公钥

上传成功后，页面会显示 **支付宝公钥**：
```
MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEA...（另一串很长的内容）
```

⚠️ **重要**：复制保存支付宝公钥，后面需要用到。

### 步骤 3：配置回调地址

1. 在应用的 **开发信息** 页面
2. 找到 **授权回调地址** 或 **网关地址**
3. 添加你的回调地址：
   ```
   https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url
   ```
4. 保存配置

### 步骤 4：添加功能

1. 进入 **产品列表**
2. 根据需要添加功能（至少需要一个）：
   - **手机网站支付**（适用于移动端）
   - **电脑网站支付**（适用于PC端）
   - **当面付**（扫码支付）
3. 提交签约（可能需要营业执照等资料）

---

## 💻 系统后台配置

### 步骤 1：登录后台

访问：`https://dujiaoka-production-c6cf.up.railway.app/admin`

### 步骤 2：进入支付方式管理

导航：**店铺管理** → **支付方式管理**

### 步骤 3：新增支付宝支付方式

点击 **新增** 按钮，填写以下信息：

---

## 📝 字段详细说明

### 1. pay_name（支付方式名称）✏️
```
支付宝支付
```
- 这是显示给用户看的名称
- 可以自定义，如：支付宝、Alipay、支付宝扫码等

---

### 2. merchant_id（商户号）🔑
```
2021001234567890
```
**这是什么**：支付宝 App ID（应用ID）

**在哪获取**：
1. 支付宝开放平台
2. 控制台 → 选择应用
3. 开发信息 → App ID

**示例格式**：
- 16位数字
- 如：`2021001234567890`

---

### 3. merchant_key（商户密钥）🔐
```
MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAwJZ...
```
**这是什么**：**支付宝公钥**（Alipay Public Key）

**⚠️ 重要**：
- **不是**应用公钥
- **不是**应用私钥
- **是**支付宝返回给你的公钥

**在哪获取**：
1. 支付宝开放平台
2. 控制台 → 选择应用
3. 开发信息 → 接口加签方式
4. 点击 **查看支付宝公钥**

**格式**：
```
MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAwJZNd...（很长的一串字符）
```

**填写时注意**：
- ✅ 复制完整的公钥内容
- ❌ 不要包含 `-----BEGIN PUBLIC KEY-----` 开头
- ❌ 不要包含 `-----END PUBLIC KEY-----` 结尾
- ❌ 不要有多余的空格或换行

---

### 4. merchant_pem（商户私钥）🔒
```
MIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAoIBAQC...
```
**这是什么**：**应用私钥**（Application Private Key）

**在哪获取**：
- 使用密钥生成工具生成的
- 是你自己保存的私钥
- **千万不要泄露给任何人**

**格式**：
```
MIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAo...（非常长的一串字符）
```

**填写时注意**：
- ✅ 复制完整的私钥内容
- ❌ 不要包含 `-----BEGIN RSA PRIVATE KEY-----` 开头
- ❌ 不要包含 `-----END RSA PRIVATE KEY-----` 结尾
- ❌ 不要有多余的空格或换行

---

### 5. pay_check（支付标识）🏷️
```
alipay
```
**这是什么**：支付方式的唯一标识符

**固定值**：`alipay`

**用途**：
- 代码中用于识别支付方式
- 图标显示
- 路由匹配

---

### 6. pay_client（支付客户端）📱
选项：
- `PC端` - 电脑网站支付
- `移动端` - 手机网站支付

**如何选择**：
- 如果你的网站主要在电脑上访问 → 选择 **PC端**
- 如果你的网站主要在手机上访问 → 选择 **移动端**
- 可以同时创建两个支付方式（一个PC端，一个移动端）

---

### 7. pay_method（支付方式）💳
选项：
- `跳转` - 跳转到支付宝页面支付
- `扫码` - 显示二维码扫码支付

**如何选择**：
- **PC端** 通常选择 **扫码**（用户用手机扫码）
- **移动端** 通常选择 **跳转**（跳转到支付宝APP）

---

### 8. pay_handleroute（处理路由）🛣️
```
/pay/alipay
```
**固定值**：必须是 `/pay/alipay`

**⚠️ 重要**：
- 不能修改
- 不能是 `/alipay` 或 `/pay/ali` 等其他值
- 必须和代码中的路由匹配

**用途**：
- 系统用于匹配支付回调
- 生成支付回调地址时使用

---

### 9. is_open（是否启用）✅
选项：
- `开启` - 用户可以选择此支付方式
- `关闭` - 用户看不到此支付方式

**建议**：配置完成后选择 **开启**

---

## 🎯 完整配置示例

```
支付方式名称：支付宝支付
商户号(merchant_id)：2021001234567890
商户密钥(merchant_key)：MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAwJZ...（支付宝公钥）
商户私钥(merchant_pem)：MIIEvgIBADANBgkqhkiG9w0BAQEFAASCBKgwggSkAgEAAo...（应用私钥）
支付标识(pay_check)：alipay
支付客户端(pay_client)：PC端
支付方式(pay_method)：扫码
处理路由(pay_handleroute)：/pay/alipay
是否启用(is_open)：✅ 开启
```

---

## ❌ 常见错误

### 错误 1：merchant_key 和 merchant_pem 填反了

**症状**：支付时提示"签名验证失败"

**原因**：
- merchant_key 应该填 **支付宝公钥**（从支付宝后台获取）
- merchant_pem 应该填 **应用私钥**（你自己生成的）

**解决**：
重新检查两个字段的内容，确保没有填反。

---

### 错误 2：密钥包含了开头结尾

**症状**：支付时报错

**原因**：
密钥中包含了 `-----BEGIN...-----` 和 `-----END...-----`

**解决**：
只保留中间的密钥内容，去掉开头结尾：

**错误示例** ❌：
```
-----BEGIN PUBLIC KEY-----
MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAwJZ...
-----END PUBLIC KEY-----
```

**正确示例** ✅：
```
MIIBIjANBgkqhkiG9w0BAQEFAAOCAQ8AMIIBCgKCAQEAwJZ...
```

---

### 错误 3：pay_handleroute 配置错误

**症状**：回调时提示"路由不匹配"

**原因**：
- pay_handleroute 填成了 `/alipay` 或其他值
- 必须是 `/pay/alipay`

**解决**：
修改为 `/pay/alipay`

---

### 错误 4：密钥生成格式不对

**症状**：签名验证失败

**原因**：
- 密钥格式选择错误
- PHP 项目应该使用 PKCS1 格式

**解决**：
1. 重新使用密钥生成工具
2. 选择 **PKCS1 (非JAVA适用)**
3. 重新生成密钥对

---

## 🧪 测试配置

配置完成后，测试步骤：

### 1. 创建测试商品
- 最小金额（如 0.01 元）
- 设置为自动发货

### 2. 创建测试订单
- 访问网站前台
- 选择商品下单
- 选择支付宝支付

### 3. 完成支付
- 扫码或跳转支付
- 使用真实支付宝账号支付

### 4. 查看结果
- 订单状态是否更新为"已完成"
- 是否收到订单邮件
- Railway 日志是否有回调记录

---

## 📞 获取帮助

如果配置过程中遇到问题：

1. **查看日志**
   - Railway Dashboard → Logs
   - 搜索"支付宝回调"

2. **检查配置**
   - 对照本文档逐项检查
   - 确认支付宝后台配置正确

3. **联系支持**
   - 提供完整的错误日志
   - 说明配置的具体步骤

---

## 🔗 相关链接

- [支付宝开放平台](https://open.alipay.com/)
- [密钥生成工具下载](https://opendocs.alipay.com/common/02kipl)
- [支付宝开放平台文档](https://opendocs.alipay.com/)
- [电脑网站支付文档](https://opendocs.alipay.com/open/270)
- [手机网站支付文档](https://opendocs.alipay.com/open/203)

---

**配置完成后记得点击"开启"并测试！** 🚀
