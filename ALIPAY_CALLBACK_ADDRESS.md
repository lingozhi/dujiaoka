# 支付宝回调地址配置说明

## 🎯 支付宝后台需要配置的地址

### 📍 重要提示

支付宝开放平台有多个地方需要配置地址，但**对于支付功能**来说，主要有两个：

---

## 1️⃣ 授权回调地址（OAuth授权）

### 这是什么？
用于支付宝登录、获取用户信息等**OAuth授权**场景，**不是支付回调**。

### 独角数卡需要配置吗？
**❌ 不需要！**

独角数卡只使用支付功能，不使用支付宝登录或授权功能。

### 如果支付宝后台强制要求填写怎么办？
可以填写你的网站首页地址：
```
https://dujiaoka-production-c6cf.up.railway.app
```

或者随便填一个合法的 HTTPS 地址（因为不会用到）。

---

## 2️⃣ 异步通知地址（支付回调）⭐ **重要！**

### 这是什么？
用户支付成功后，支付宝服务器会向这个地址发送通知，告诉系统订单已支付。

### 在哪里配置？

#### 位置 A：应用网关（推荐）
1. 支付宝开放平台：https://open.alipay.com/
2. 控制台 → 选择应用 → **开发信息**
3. 找到 **应用网关** 或 **网关地址**
4. 填写：
   ```
   https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url
   ```

#### 位置 B：产品功能设置
某些支付产品（如电脑网站支付、手机网站支付）也有独立的回调地址配置：

1. 控制台 → 产品列表 → 选择产品（如"电脑网站支付"）
2. 点击 **设置** 或 **查看**
3. 找到 **异步通知地址** 或 **Notify URL**
4. 填写：
   ```
   https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url
   ```

### ⚠️ 注意事项

1. **必须是 HTTPS**
   - ❌ `http://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url`
   - ✅ `https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url`

2. **必须公网可访问**
   - ❌ `http://localhost/pay/alipay/notify_url`
   - ❌ `http://127.0.0.1/pay/alipay/notify_url`
   - ✅ Railway 提供的域名是公网可访问的

3. **路径必须正确**
   - ✅ `/pay/alipay/notify_url`（正确）
   - ❌ `/alipay/notify_url`（错误）
   - ❌ `/pay/alipay/callback`（错误）

---

## 3️⃣ 同步返回地址（用户跳转）

### 这是什么？
用户支付完成后，浏览器跳转回商户网站的地址。

### 在哪里配置？
**不需要在支付宝后台配置！**

系统代码中已经自动配置了：
```php
'return_url' => url('detail-order-sn', ['orderSN' => $this->order->order_sn])
```

生成的地址：
```
https://dujiaoka-production-c6cf.up.railway.app/detail-order-sn/订单号
```

---

## 📋 配置清单总结

| 配置项 | 是否必须 | 填写内容 |
|-------|---------|---------|
| **授权回调地址** | ❌ 不需要 | 随便填或填首页地址 |
| **异步通知地址** | ✅ **必须** | `https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url` |
| **同步返回地址** | ❌ 代码自动配置 | 无需手动配置 |

---

## 🔍 如何找到配置位置？

### 方法 1：搜索"网关"
支付宝后台搜索关键词：**网关**、**应用网关**、**Gateway**

### 方法 2：查看产品配置
1. 控制台 → 产品列表
2. 找到你签约的产品（如"电脑网站支付"）
3. 点击查看或设置
4. 找到回调相关配置

### 方法 3：查看开发文档
在应用的开发信息页面，通常会有文档链接或配置说明。

---

## 🧪 验证配置是否正确

### 步骤 1：创建测试订单
在网站上创建一个小额订单（如 0.01 元）

### 步骤 2：完成支付
使用真实支付宝账号完成支付

### 步骤 3：查看订单状态
- ✅ 如果订单状态变为"已完成"，说明回调配置正确
- ❌ 如果订单状态还是"待支付"，说明回调有问题

### 步骤 4：查看 Railway 日志
访问 Railway Dashboard → Logs，搜索"支付宝回调"：

**配置正确的日志**：
```
[INFO] 支付宝回调接收 {...}
[INFO] 支付宝回调签名验证成功 {...}
[INFO] 支付宝订单处理成功 {...}
```

**配置错误的情况**：
- 完全没有日志 → 支付宝没有发送回调（回调地址配置错误）
- 有日志但验证失败 → 密钥配置错误

---

## 📸 支付宝后台配置截图说明

由于不同应用类型界面可能不同，以下是常见位置：

### 界面 A：应用网关
```
应用信息
├── 开发信息
│   ├── App ID
│   ├── 接口加签方式（密钥/证书）
│   └── 应用网关 ← 在这里填写回调地址
└── ...
```

### 界面 B：产品配置
```
产品列表
├── 电脑网站支付
│   ├── 查看
│   └── 异步通知地址 ← 也可以在这里填写
└── ...
```

---

## ❓ 常见问题

### Q1: 找不到"授权回调地址"配置项？

**答**：如果找不到，说明你的应用类型不需要配置。这是正常的，因为支付功能不需要授权回调。

### Q2: "异步通知地址"和"应用网关"有什么区别？

**答**：
- **应用网关**：应用级别的配置，所有产品共用
- **异步通知地址**：产品级别的配置，每个产品独立

**建议**：如果有应用网关，填应用网关就够了。

### Q3: 配置了回调地址，但还是收不到回调？

**可能原因**：
1. Railway 的域名不能访问（检查是否能 ping 通）
2. 密钥配置错误（支付宝验证签名失败）
3. 路由配置错误（检查系统后台的 pay_handleroute）

**排查步骤**：
1. 在浏览器直接访问：`https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url`
   - 应该返回 `error` 或其他内容（不是 404）
2. 查看 Railway 日志是否有任何回调记录
3. 检查支付宝后台的回调日志（部分平台提供）

### Q4: 可以填 IP 地址吗？

**答**：❌ 不可以。支付宝要求回调地址必须是域名，而且必须是 HTTPS。

---

## 🎯 快速配置指南

### 1. 你的回调地址是：
```
https://dujiaoka-production-c6cf.up.railway.app/pay/alipay/notify_url
```

### 2. 复制这个地址到：
- 支付宝开放平台 → 应用 → 开发信息 → 应用网关

### 3. 确认：
- ✅ 使用 HTTPS
- ✅ 域名正确
- ✅ 路径是 `/pay/alipay/notify_url`

### 4. 保存配置

### 5. 测试支付

---

## 📞 需要帮助？

如果按照上面的步骤配置后还有问题：

1. **检查 Railway 日志**
   - 搜索"支付宝回调"
   - 查看是否有错误信息

2. **提供以下信息**
   - 支付宝后台配置的截图
   - Railway 日志中的错误信息
   - 订单号和支付状态

3. **我会帮你排查**
   - 分析具体原因
   - 给出解决方案

---

**记住：对于支付功能，最重要的是配置"异步通知地址"！** 🎯
